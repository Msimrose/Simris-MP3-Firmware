Pact MP-1 icon set
==================
Two forms of each glyph:
  <name>.svg        square-matrix (16x16 soft cells, matches the 16-seg LED bar) - the aesthetic
  <name>-solid.svg  clean solid vector - guaranteed shape, best for tiny sizes / editing
battery.svg         dynamic battery shell + proportional fill (62% shown; edit the inner rect width)

viewBox 0 0 96 96, single colour #F4F3EF (recolour freely). Transparent background.
Drop straight into Illustrator (File > Place, or open). Grid = 16 so cells line up with the LED bar.
